{
  "name": "reactor-hub",
  "private": true,
  "version": "1.0.0",
  "description": "Reactor Hub — a live control room for the PrajwalShete/Live GitHub repo. Frontend-only React; fetches the repo fresh on every refresh.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
